import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  icon: {
    textAlign: 'right',
  },
  addicon: {
    textAlign: 'right',
    width: '5%',
  },
});

export default style;
