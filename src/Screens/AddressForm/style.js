import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  heading: {
    size: 'lg',
    fontWeight: '600',
    color: 'coolGray.800',
  },
  input: {
    size: 'xl',
    border: '0.5',
  },
});

export default style;
